services:
  clawbot:
    build: .
    container_name: clawbot
    restart: unless-stopped
    env_file: .env
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"
